.DS_Store
*.swp
*.swo
.idea/
.vscode/
node_modules/

# Local-only notes (deployment playbooks, scratch, secrets) — never pushed
*.local.md
*.local.txt
.private/

# Operational data (real candidate names, DM drafts, CRM) — never pushed
outreach/
